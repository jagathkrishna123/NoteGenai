import { FilePenLineIcon, PencilIcon, PlusIcon, TrashIcon, UploadCloud, UploadCloudIcon, XIcon } from "lucide-react";
import { useEffect, useState } from "react";
import { dummyNotesData } from "../assets/assets";
import { useNavigate } from "react-router-dom";

const Dashboard = () => {

  const navigate = useNavigate()
  const colors = ["#9333ea", "#d97706", "#dc2626", "#0284c7", "#16a34a"];

  const [allNotes, setAllNotes] = useState([]); //allresume
  const [showCreateNote, setShowCreateNote] = useState(false);//showcreateresume
  const [showUploadSyllabus, setShowUploadSyllabus] = useState(false);//showuploadResume
  const [topic, setTopic] = useState('')//title
  const [note, setNote] = useState(null)//resume
  const [editNoteId, setEditNoteId] = useState('')//editResumeId

  const loadAllNotes = async () => {
    setAllNotes(dummyNotesData);
  };

  useEffect(() => {
    loadAllNotes();
  }, []);

  const handleCreateNote = async (event) => {
    event.preventDefault()
    setShowCreateNote(false)
    navigate(`/app/notegen/note123`)
  }

  const handleUploadSyllabus = async (event) => {
    event.preventDefault()
    setShowUploadSyllabus(false)
    navigate(`/app/notegen/note123`)
  }
  return (
    <div>
      <div className="max-w-7xl mx-auto px-4 py-8">
        <p className="text-2xl font-medium mb-6 bg-gradient-to-r from-slate-600 to-slate-700 bg-clip-text text-transparent sm:hidden">
          Welcome, Joe Doe
        </p>

        <div className="flex gap-4 ">
          <button onClick={()=> setShowCreateNote(true)} className="w-full bg-white sm:max-w-36 h-48 flex flex-col items-center justify-center rounded-lg gap-2 text-slate-600 border border-dashed border-slate-300 group hover:border-purple-500 hover:shadow-lg transition-all duration-300 cursor-pointer">
            <PlusIcon className="size-11 transition-all duration-300 p-2.5 bg-gradient-to-br from-purple-300 to-purple-500 text-white rounded-full" />

            <p className="text-sm group-hover:text-purple-600 transition-all duration-300">
              Create Note
            </p>
          </button>
          <button onClick={()=> setShowUploadSyllabus(true)} className="w-full bg-white sm:max-w-36 h-48 flex flex-col items-center justify-center rounded-lg gap-2 text-slate-600 border border-dashed border-slate-300 group hover:border-purple-500 hover:shadow-lg transition-all duration-300 cursor-pointer">
            <UploadCloudIcon className="size-11 transition-all duration-300 p-2.5 bg-gradient-to-br from-purple-300 to-purple-500 text-white rounded-full" />

            <p className="text-sm group-hover:text-purple-600 transition-all duration-300">
              Upload Syllabus
            </p>
          </button>
        </div>
        <hr className="border-slate-300 my-6 sm:w-[305px]" />
        <div className="grid grid-cols-2 sm:flex flex-wrap gap-4">
          {allNotes.map((note, index) => {
            const baseColor = colors[index % colors.length];
            return (
              <button
                key={index}
                className="relative w-full sm:max-w-36 h-48 flex flex-col items-center justify-center rounded-lg gap-2 border group hover:shadow-lg transition-all duration-300 cursor-pointer"
                style={{
                  background: `linear-gradient(135deg, ${baseColor}10, ${baseColor}40)`,
                  borderColor: baseColor + "40",
                }}
              >
                <FilePenLineIcon
                  className="size-7 group-hover:scale-105 transition-all "
                  style={{ color: baseColor }}
                />

                <p
                  className="text-sm group-hover:scale-105 transition-all px-2 text-center"
                  style={{ color: baseColor }}
                >
                  {note.title}
                </p>

                <p className="absolute bottom-1 text-[11px] text-slate-400 group-hover:text-slate-500 transition-all duration-300 px-2 text-center" style={{color:baseColor + '90'}}>
                  Updated on {new Date(note.updatedAt).toLocaleDateString()}
                </p>
                <div className="absolute top-1 right-1 group-hover:flex items-center hidden">
                    <TrashIcon className="size-7 p-1.5 hover:bg-white/50 rounded text-slate-700 transition-colors"/>
                    <PencilIcon className="size-7 p-1.5 hover:bg-white/50 rounded text-slate-700 transition-colors"/>
                </div>
              </button>
            );
          })}
        </div>

        {showCreateNote && (
            <form onSubmit={handleCreateNote} onClick={()=> setShowCreateNote(false)} className="fixed inset-0 bg-black/70 backdrop-blur bg-opacity-50 z-10 flex items-center justify-center">
                <div onClick={e => e.stopPropagation()} className="relative bg-slate-50 border shadow-md rounded-lg w-full max-w-sm p-6">
                    <h2 className="text-xl font-bold mb-4">Create a Note</h2>
                    <input onChange={(e)=> setTopic(e.target.value)} value={topic} type="text" placeholder="Enter your Topic" className="w-full px-4 py-2 mb-4 focus:border-green-600 ring-green-600 required:"/>
                    <button className="w-full py-2 bg-green-600 text-white rounded hover:bg-green-700 transition-colors">Create Note</button>
                    <XIcon onClick={()=> {setShowCreateNote(false); setTopic('')}} className="absolute top-4 right-4 text-slate-400 hover:text-slate-600 cursor-pointer transition-colors"/>
                </div>
            </form>
        )}

        {showUploadSyllabus && (
            <form onSubmit={handleUploadSyllabus} onClick={()=> setShowUploadSyllabus(false)} className="fixed inset-0 bg-black/70 backdrop-blur bg-opacity-50 z-10 flex items-center justify-center">
                <div onClick={e => e.stopPropagation()} className="relative bg-slate-50 border shadow-md rounded-lg w-full max-w-sm p-6">
                    <h2 className="text-xl font-bold mb-4">Upload Syllabus</h2>
                    <input onChange={(e)=> setTopic(e.target.value)} value={topic} type="text" placeholder="Enter your Topic" className="w-full px-4 py-2 mb-4 focus:border-green-600 ring-green-600 required:"/>
                    <div>
                        <label htmlFor="note-input" className="block text-sm text-slate-700">
                            Select Syllabus file
                            <div className='flex flex-col items-center justify-center gap-2 border group text-slate-400 border-slate-400 border-dashed rounded-md p-4 py-10 my-4 hover:border-green-500 hover:text-green-700 cursor-pointer transition-colors'>
                                {note ? (
                                    <p className='text-green-700'>{note.name}</p>
                                ) : (
                                    <>
                                    <UploadCloud className='size-14 stroke-1' />
                                    <p>Upload syllabus</p>
                                    </>
                                )}
                            </div>

                        </label>
                        <input onChange={(e)=> setNote(e.target.files[0])} type="file" id="note-input" accept=".pdf" hidden />
                    </div>
                    <button className="w-full py-2 bg-green-600 text-white rounded hover:bg-green-700 transition-colors">Upload Resume</button>
                    <XIcon onClick={()=> {setShowUploadSyllabus(false); setTopic('')}} className="absolute top-4 right-4 text-slate-400 hover:text-slate-600 cursor-pointer transition-colors"/>
                </div>
            </form>
        )}
      </div>
    </div>
  );
};

export default Dashboard;
333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333333


import { FilePenLineIcon, PencilIcon, PlusIcon, TrashIcon, UploadCloud, UploadCloudIcon, XIcon } from "lucide-react";
import { useEffect, useState } from "react";
import { dummyNotesData } from "../assets/assets";
import { useNavigate } from "react-router-dom";

const Dashboard = () => {

  const navigate = useNavigate()
  const colors = ["#9333ea", "#d97706", "#dc2626", "#0284c7", "#16a34a"];

  const [allNotes, setAllNotes] = useState([]); //allresume
  const [showCreateNote, setShowCreateNote] = useState(false);//showcreateresume
  const [showUploadSyllabus, setShowUploadSyllabus] = useState(false);//showuploadResume
  const [topic, setTopic] = useState('')//title
  const [note, setNote] = useState(null)//resume
  const [editNoteId, setEditNoteId] = useState('')//editResumeId

  const loadAllNotes = async () => {
    setAllNotes(dummyNotesData);
  };

  useEffect(() => {
    loadAllNotes();
  }, []);

  const handleCreateNote = async (event) => {
    event.preventDefault()
    setShowCreateNote(false)
    navigate(`/app/notegen/note123`)
  }

  const handleUploadSyllabus = async (event) => {
    event.preventDefault()
    setShowUploadSyllabus(false)
    navigate(`/app/notegen/note123`)
  }

  const handleEditPdfTitle = async (event) => {
    event.preventDefault()
  }
  const handleDeletePdfNote = async (noteId) => {
    const confirm = window.confirm('Are you sure you want to delete this Note?')
    if(confirm) {
        setAllNotes(prev => prev.filter(note._id !== noteId))
    }
  }
  return (
    <div>
      <div className="max-w-7xl mx-auto px-4 py-8">
        <p className="text-2xl font-medium mb-6 bg-gradient-to-r from-slate-600 to-slate-700 bg-clip-text text-transparent sm:hidden">
          Welcome, Joe Doe
        </p>

        <div className="flex gap-4 ">
          <button onClick={()=> setShowCreateNote(true)} className="w-full bg-white sm:max-w-36 h-48 flex flex-col items-center justify-center rounded-lg gap-2 text-slate-600 border border-dashed border-slate-300 group hover:border-purple-500 hover:shadow-lg transition-all duration-300 cursor-pointer">
            <PlusIcon className="size-11 transition-all duration-300 p-2.5 bg-gradient-to-br from-purple-300 to-purple-500 text-white rounded-full" />

            <p className="text-sm group-hover:text-purple-600 transition-all duration-300">
              Create Note
            </p>
          </button>
          <button onClick={()=> setShowUploadSyllabus(true)} className="w-full bg-white sm:max-w-36 h-48 flex flex-col items-center justify-center rounded-lg gap-2 text-slate-600 border border-dashed border-slate-300 group hover:border-purple-500 hover:shadow-lg transition-all duration-300 cursor-pointer">
            <UploadCloudIcon className="size-11 transition-all duration-300 p-2.5 bg-gradient-to-br from-purple-300 to-purple-500 text-white rounded-full" />

            <p className="text-sm group-hover:text-purple-600 transition-all duration-300">
              Upload Syllabus
            </p>
          </button>
        </div>
        <hr className="border-slate-300 my-6 sm:w-[305px]" />
        <div className="grid grid-cols-2 sm:flex flex-wrap gap-4">
          {allNotes.map((note, index) => {
            const baseColor = colors[index % colors.length];
            return (
              <button onClick={()=> navigate(`/app/builder/${note._id}`)}
                key={index}
                className="relative w-full sm:max-w-36 h-48 flex flex-col items-center justify-center rounded-lg gap-2 border group hover:shadow-lg transition-all duration-300 cursor-pointer"
                style={{
                  background: `linear-gradient(135deg, ${baseColor}10, ${baseColor}40)`,
                  borderColor: baseColor + "40",
                }}
              >
                <FilePenLineIcon
                  className="size-7 group-hover:scale-105 transition-all "
                  style={{ color: baseColor }}
                />

                <p
                  className="text-sm group-hover:scale-105 transition-all px-2 text-center"
                  style={{ color: baseColor }}
                >
                  {note.title}
                </p>

                <p className="absolute bottom-1 text-[11px] text-slate-400 group-hover:text-slate-500 transition-all duration-300 px-2 text-center" style={{color:baseColor + '90'}}>
                  Updated on {new Date(note.updatedAt).toLocaleDateString()}
                </p>
                <div onClick={e=> e.stopPropagation()} className="absolute top-1 right-1 group-hover:flex items-center hidden">
                    <TrashIcon onClick={()=> handleDeletePdfNote(note._id)} className="size-7 p-1.5 hover:bg-white/50 rounded text-slate-700 transition-colors"/>
                    <PencilIcon onClick={()=> {setEditNoteId(note._id); setTopic(note.title)}} className="size-7 p-1.5 hover:bg-white/50 rounded text-slate-700 transition-colors"/>
                </div>
              </button>
            );
          })}
        </div>

        {showCreateNote && (
            <form onSubmit={handleCreateNote} onClick={()=> setShowCreateNote(false)} className="fixed inset-0 bg-black/70 backdrop-blur bg-opacity-50 z-10 flex items-center justify-center">
                <div onClick={e => e.stopPropagation()} className="relative bg-slate-50 border shadow-md rounded-lg w-full max-w-sm p-6">
                    <h2 className="text-xl font-bold mb-4">Create a Note</h2>
                    <input onChange={(e)=> setTopic(e.target.value)} value={topic} type="text" placeholder="Enter your Topic" className="w-full px-4 py-2 mb-4 focus:border-green-600 ring-green-600 required:"/>
                    <button className="w-full py-2 bg-green-600 text-white rounded hover:bg-green-700 transition-colors">Create Note</button>
                    <XIcon onClick={()=> {setShowCreateNote(false); setTopic('')}} className="absolute top-4 right-4 text-slate-400 hover:text-slate-600 cursor-pointer transition-colors"/>
                </div>
            </form>
        )}

        {showUploadSyllabus && (
            <form onSubmit={handleUploadSyllabus} onClick={()=> setShowUploadSyllabus(false)} className="fixed inset-0 bg-black/70 backdrop-blur bg-opacity-50 z-10 flex items-center justify-center">
                <div onClick={e => e.stopPropagation()} className="relative bg-slate-50 border shadow-md rounded-lg w-full max-w-sm p-6">
                    <h2 className="text-xl font-bold mb-4">Upload Syllabus</h2>
                    <input onChange={(e)=> setTopic(e.target.value)} value={topic} type="text" placeholder="Enter your Topic" className="w-full px-4 py-2 mb-4 focus:border-green-600 ring-green-600 required:"/>
                    <div>
                        <label htmlFor="note-input" className="block text-sm text-slate-700">
                            Select Syllabus file
                            <div className='flex flex-col items-center justify-center gap-2 border group text-slate-400 border-slate-400 border-dashed rounded-md p-4 py-10 my-4 hover:border-green-500 hover:text-green-700 cursor-pointer transition-colors'>
                                {note ? (
                                    <p className='text-green-700'>{note.name}</p>
                                ) : (
                                    <>
                                    <UploadCloud className='size-14 stroke-1' />
                                    <p>Upload syllabus</p>
                                    </>
                                )}
                            </div>

                        </label>
                        <input onChange={(e)=> setNote(e.target.files[0])} type="file" id="note-input" accept=".pdf" hidden />
                    </div>
                    <button className="w-full py-2 bg-green-600 text-white rounded hover:bg-green-700 transition-colors">Upload Resume</button>
                    <XIcon onClick={()=> {setShowUploadSyllabus(false); setTopic('')}} className="absolute top-4 right-4 text-slate-400 hover:text-slate-600 cursor-pointer transition-colors"/>
                </div>
            </form>
        )}

        {editNoteId && (
            <form onSubmit={handleEditPdfTitle} onClick={()=> setEditNoteId("")} className="fixed inset-0 bg-black/70 backdrop-blur bg-opacity-50 z-10 flex items-center justify-center">
                <div onClick={e => e.stopPropagation()} className="relative bg-slate-50 border shadow-md rounded-lg w-full max-w-sm p-6">
                    <h2 className="text-xl font-bold mb-4">Edit Note title</h2>
                    <input onChange={(e)=> setTopic(e.target.value)} value={topic} type="text" placeholder="Enter your Topic" className="w-full px-4 py-2 mb-4 focus:border-green-600 ring-green-600 required:"/>
                    <button className="w-full py-2 bg-green-600 text-white rounded hover:bg-green-700 transition-colors">Update</button>
                    <XIcon onClick={()=> {setEditNoteId(""); setTopic('')}} className="absolute top-4 right-4 text-slate-400 hover:text-slate-600 cursor-pointer transition-colors"/>
                </div>
            </form>
        )}

      </div>
    </div>
  );
};

export default Dashboard;
///////////////////////////////////////////////////////////////////

